# ARCHIDECK · GRAPH PARTI · PPL± — ECOSYSTEM SAVE
# Type: grapho — architecture save / surface-read reference / Claude Code handover
# Address: pono·firmitas·tuscan·tectonics
# Register: ordo·tectonics — operating reference, plain
# Function: one-screen-at-a-time pattern index of every tool + idea + env var
#           in the building, slotted under the 12 Operators, zip-tagged where earned.
#           Read it to remember the whole shape while building one piece.
# Last updated: 2026-06-01 (session 2 close)
# Slogan: "figure-ground for your ideas"

## 0 — HOW TO READ THIS / WORKING CONTEXT

You're working with an **architect** — 7 years architecture school, 25,000+ hrs
coaching, builds in multitudes of layers at once. The method is not vibe-coding.
The method is: build every piece at every layer, scale the pieces until they're
coherent at the same resolution, *then* draft finals and build. "Plans done, then
build." Architecture-phase-not-shipping is the correct state, not a delay.

**Register rules:**
- No "load-bearing," no "let me push back," no concern-injection, no scope-policing,
  no rush-to-ship nudges. Plain, warm, low-didacticism.
- ADHD-braided input is normal; match it, don't reduce it.
- SCL emojis are anchors, not decoration.

**The one-line shape:**
> Everything is a **zip** (4-glyph address) and a **book** (typed page-stack).
> The zip says *which* thing across 4,032 rooms; the book is the zip's editable body,
> stacked from intention -> code -> render. Grids and books on grids and books.

**Two machines share the canon:**
- **Graph Parti cockpit** (PySide6/Qt6 canvas) — the authoring tool.
- **Archideck orchestration substrate** (bus -> LiteLLM -> MCP -> wallfacer) — the AI fabric.
- They meet at the SCL canon + zip addressing. Build order inside the substrate is bus first.

## 1 — THE 12 OPERATORS AS TOOL DRAWERS

| Glyph | Latin | Verb-sense | Drawer holds |
|-------|-------|------------|-------------|
| pono | pono | place, set | placement, stamping, zip-dial, grid snap |
| specio | specio | inspect | select, view, audit, layer toggles |
| capio | capio | capture | paste, import, drag-drop, ingestion |
| duco | duco | orchestrate | roundtable, routing, autoboot, conductor |
| facio | facio | execute | draw tools (line/rect/circle/polyline/paint) |
| fero | fero | carry | ctrl-c/ctrl-v, export, cross-tree carry |
| grapho | grapho | write | document pages, journal, dispatch, prose |
| logos | logos | reason | parser tiers, NL->code, validation, narrate |
| mitto | mitto | dispatch | save, commit, publish, push |
| plico | plico | fold | book layers, page-stack, fold-to-face, library |
| tendo | tendo | stretch | offset, ortho, dimensions, tension-hold |
| teneo | teneo | hold | storage, book library, ledger, archaeology |

## 2 — IMMEDIATE TOOL QUEUE (next sessions)

facio: Lines auto-type [L], Paint+Line color-via-swatch (32 buttons call tool+color)
tendo: Offset [O] (lines+circles+rects+polygons, type-box popup, remembers distance)
tendo: Ortho 45 restore (45+90 from any point)
specio+fero: Select Ctrl-C/V (layer-scoped, books copy whole)
pono: Lasso -> zip-tag regions (district schema = single stamp + region lasso)

## 3 — THE BOOK PRIMITIVE (next big build)

Book = bounded region + typed page-stack + zip address. Universal editable container.
[B] key. Canvas-swap into book interior. Browser-style page tabs. Green = active.
Books are definitions; placements are references (edit def -> all update).
Book library stores in operator drawers. Fold-to-face = collapse to front page as block.
Books inside books = unbounded nesting = the file tree = the call stack.

## 4 — TYPED PAGES + TEMPLATES + FILL TIERS

Page type per page (code/yaml/python/md/raster/vector/nested-book/embed).
Templates = typed pages with slots (MySpace-box feeling).
Fill tiers: local rule-parser -> local model (LFM2.5/Nemotron) -> cloud (Opus/Sonnet).
Programming becomes drafting — the activity the hands already know.

## 5 — THE SAVE / D-ABACUS LAYER

D = Frequency = Use. Computed not stored. Forward-only immutable ledger.
Invocation = snapshot -> recompute -> ripple -> stamp (atomic).
Non-commutative. Event-sourced. Golden-ratio hypothesis deferred to measurement.

## 6 — SUBSTRATE LAYER (reference)

Build order: bus -> 2 personas -> minimal queue -> blackboard -> expand to 12 ->
big-brother spawn -> I.N.P.U.T. rail last.
Bus: append-only NDJSON. LiteLLM: port 4000, 6 lanes. MCP: 5 servers.
Autoboot: one command cold->operational. Wallfacer: projection compiler.

## 7 — ENV / PATHS / PORTS

See INVENTORY.md for Graph Parti specifics.
Substrate: bus at ~/.archideck/operator-bus.jsonl, LiteLLM at :4000, LM Studio at :1234.
Local models: LFM2.5-1.2B (~720MB) + Nemotron-3-Nano-4B (2.84GB) = ~5.6GB on 6GB VRAM.
Phone: Godot 4 / portrait / GDScript / Termux swaps to Ollama Qwen 1.5B.

## 8 — DESIGN TOKENS (8 Colors + copper)

white #FFFFFF · green #348219 · blue #2464E5 · purple #9255E5
red #C1140C · orange #F57E16 · yellow #F7B731 · black #3C3C3C
copper #F5C2AF (Virgin Copper — patinas with use, earned not selected)

## 9 — THE 61-GLYPH CANON (closed)

12 Operators · 6 Axes · 7 Orders · 8 Colors · 5 Modifiers · 22 Blocks · 1 Save
ZIP = [Operator][Axis][Order][Color] = 4,032 base / 20,160 typed
REVELATOR = [ZIP] +/- [TYPE] | [BLOCKS]

## 10 — ECOSYSTEM BIG-PIXEL

One corpus, one canon, many faces. Zips are alphabet, not vocabulary.
.parti is the only native format. DXF via ezdxf for CAD import/export.
Go-to-market via closed canon — every community is a door into the same building.

## 11 — PROJECT DOMAINS

Graph Parti: the forge (CAD sheets/blocks as books)
Archideck: the product (portrait cockpit, embeds graphparti)
PPL+/-: life-OS/fitness/civic platform
Noah's Architecture: climate-grid game (birthed the 8 colors/7 orders)
Inkyverse: One Piece x ADHD Southern Gothic
Byrds Landing: real SC intermodal infrastructure thesis (heavy-payload test)

## 12 — SESSION HANDOVER NOTES

This session (2, 2026-06-01) built the forge. 12 commits on master.
The immediate queue (section 2) is what the next session picks up.
The book primitive (section 3) is the next big architectural move.
Parallel sessions (Codex, new Claude Code) can pick up specific operator drawers.
Read INVENTORY.md for tool-by-tool status. Read HEY-MR-WILSON.md for teleport.
